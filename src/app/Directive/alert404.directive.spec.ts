import { Alert404Directive } from './alert404.directive';

describe('Alert404Directive', () => {
  it('should create an instance', () => {
    const directive = new Alert404Directive();
    expect(directive).toBeTruthy();
  });
});
